# Terraform Website Hosting on EC2

This project demonstrates how to host a **simple website on an EC2 instance** using Terraform.  
It includes a security group for HTTP/SSH access and installs Apache2 via `user_data`.
__

Outputs:

Public IP of the EC2 instance is displayed

Apache2 serves the website and can be accessed via the public IP

