# Terraform EC2 under VPC Project

This project demonstrates how to create a **single EC2 instance under a custom VPC** using Terraform.  
It includes security groups to allow SSH and HTTP access.

--
Outputs:

Public IP of the EC2 instance will be displayed

Apache2 (or other software if configured) should be running
