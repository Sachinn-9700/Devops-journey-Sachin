# docker run – Deep Dive

`docker run` is a combination of:
- docker pull
- docker create
- docker start

## Common Flags
- -i : interactive
- -t : terminal
- -d : detached mode
- --rm : auto remove container
- -p : port mapping
- -v : volume mount

